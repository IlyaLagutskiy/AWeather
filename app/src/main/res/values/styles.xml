<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="textTodayStyle" parent="android:Widget.TextView">
        <item name="android:textSize">15sp</item>
        <item name="android:textColor">@color/colorCurrentTemperature</item>
        <item name="android:gravity">center</item>
    </style>

    <style name="textWeatherTableLeft" parent="android:Widget.TextView">
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:textSize">17sp</item>
        <item name="android:textColor">@color/colorCurrentTemperature</item>
        <item name="android:gravity">center|left</item>
        <item name="android:layout_weight">1</item>
    </style>

    <style name="textWeatherTableRight" parent="android:Widget.TextView">
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:textSize">17sp</item>
        <item name="android:textColor">@color/colorCurrentTemperature</item>
        <item name="android:gravity">center|right</item>
        <item name="android:layout_weight">1</item>
    </style>

</resources>
